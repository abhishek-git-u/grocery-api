version: '3.8'
services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: grocery_store
    ports:
      - "3306:3306"
  app:
    build: .
    depends_on:
      - db
    ports:
      - "8080:8080"
